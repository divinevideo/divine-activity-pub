{
  "name": "divine-activity-pub",
  "version": "0.1.0",
  "description": "Divine ActivityPub Gateway — projects Divine (Nostr) videos into the fediverse as @user@divine.video actors",
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.42.2"
  }
}
